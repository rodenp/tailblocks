import { createBoard } from "@wixc3/react-board";
import PricingC from "../../../icons/pricing/b";

export default createBoard({
  name: "PricingC",
  Board: () => <PricingC />,
  isSnippet: true,
});
